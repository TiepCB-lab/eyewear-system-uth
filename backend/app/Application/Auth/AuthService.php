<?php

namespace App\Application\Auth;

class AuthService
{
    // TODO: [M1-IDENTITY] register(array $data): User -> create user, hash password, assign customer role
    // TODO: [M1-IDENTITY] login(string $email, string $password): array -> validate credentials, return token
    // TODO: [M1-IDENTITY] logout(User $user): void -> revoke tokens
    // TODO: [M1-IDENTITY] forgotPassword(string $email): void -> send reset link
    // TODO: [M1-IDENTITY] resetPassword(string $token, string $password): void -> update password
}
