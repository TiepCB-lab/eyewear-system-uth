// TODO: [M1-IDENTITY] LoginPage component
// - Email/password form with validation
// - "Forgot password?" link
// - "Register" link
// - Submit -> call authService.login()
// - On success -> redirect to home, store token in context/store

export default function LoginPage() {
  return <div>LoginPage - TODO</div>;
}
